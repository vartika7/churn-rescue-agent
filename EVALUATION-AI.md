# Evaluation — grading the AI investigation layer

Generated by `npm run evaluate:ai`. It grades investigations already stored in
`investigations`, so it spends no model quota and every number refers to a
response that actually happened.

## What this measures, and what it refuses to

Grading generated prose is the hard half of this project. The obvious questions
— is this root cause *right*, is this recommendation *wise* — need a human or a
second model to judge, and neither is reproducible. So everything below is
checked mechanically against the evidence the model was handed.

**There is deliberately no root-cause accuracy score.** `expected_root_cause`
exists in the dataset, but scoring prose against prose by string similarity
would produce a number that moves with phrasing rather than correctness. A
metric that looks rigorous and measures nothing is worse than an honest gap.

Ground truth is never an input to the model. It is compared against output
afterwards, which is the entire difference between grading and leaking.

## Results

Graded **6** stored investigations from a real model.

Offline placeholders are excluded. That provider composes its citations from
the evidence package, so it scores a perfect grounding rate by construction and
can never produce a phantom entity — including it would pad the denominator
with cases that cannot fail.

| Metric | Result | What a failure would mean |
| --- | --- | --- |
| Structured-output validity | 6/6 | The response could not be parsed into the required shape |
| Fully grounded | 6/6 (mean 1.000) | A citation pointed at evidence that was never supplied |
| Free of phantom entities | 6/6 | A ticket id or date was named in prose but is absent from the package |
| Uncited claims | 0 | A claim asserted with nothing behind it |
| Engaged the counter-case | 6/6 | Contradicting evidence was available and ignored |
| Aligned with the risk band | 6/6 | — |
| Escalated beyond the band | 0/6 | Not a failure; see below |
| De-escalated below the band | 0/6 | Talking a CSM out of looking at a flagged account |
| Uncertainty claimed correctly | 6/6 | It invented a root cause on thin evidence, or refused on rich evidence |
| Mean distinct evidence cited | 9.7 of ~12 available | A low number means it fixated on one signal |

### Per account

| Account | Model | Valid | Grounding | Phantoms | Counter-case | Action | Band | Insufficient |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| C001 | gemini-3.5-flash-lite | yes | 1.00 | none | used | monitor | medium (33) | no |
| C002 | gemini-3.5-flash-lite | yes | 1.00 | none | used | monitor | medium (26) | no |
| C027 | gemini-3.5-flash-lite | yes | 1.00 | none | used | monitor | medium (36) | no |
| C035 | gemini-3.5-flash-lite | yes | 1.00 | none | used | intervene | high (70) | no |
| C038 | gemini-3.5-flash-lite | yes | 1.00 | none | used | intervene | high (61) | no |
| C049 | gemini-3.5-flash-lite | yes | 1.00 | none | used | monitor | medium (26) | no |

## Recommendation against the human grading

Agreed on **3 of 6** (50%).

| `expected_recommendation` | Model said | Accounts |
| --- | --- | --- |
| no_action_needed | monitor ← | 3 |
| intervene | intervene | 2 |
| monitor | monitor | 1 |

Disagreement is not automatically a model error. `expected_recommendation` was
written with knowledge of the outcome; the model is blind to it by design, and
the investigations are only run on accounts the engine flagged, so the
comparison is over a deliberately skewed slice.

## What these numbers are not

**A small sample.** A handful of accounts on synthetic data. The grounding and
phantom checks are absolute — a single fabrication would show — but the
agreement figures are too small to carry a trend.

**Not a measure of usefulness.** Every check here asks whether a claim is
*supported*, not whether it is *insightful*. A model that only ever restated
the evidence list would score perfectly on all of it. That gap is the honest
limit of mechanical grading, and the offline provider exists partly to make it
visible: it scores well here and is deliberately not an investigation.

**Only the accounts the engine flagged.** Low-risk accounts are not
investigated, so this says nothing about behaviour on a quiet account.

